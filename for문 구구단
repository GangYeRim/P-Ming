#include <stdio.h>
int main()
{
  int input; //변수 선언

  scanf("%d", &input);//변수 입력 받음

  printf("===%d단===\n", input);//구구단 입력
  for (int i = 1; i <= 9; i++)
  {
    printf("%d*%d = %d\n", input, i, input * i);//출력
    }
  return 0;
  }
  
